{
  "services": [
    {
      "type": "web",
      "name": "guardian-eye-backend",
      "env": "node",
      "buildCommand": "cd guardian-eye-security-main/backend && npm install",
      "startCommand": "cd guardian-eye-security-main/backend && npm start",
      "healthCheckPath": "/health",
      "envVars": [
        { "key": "NODE_ENV", "value": "production" },
        { "key": "PORT", "value": "3001" }
      ]
    },
    {
      "type": "web",
      "name": "guardian-eye-frontend",
      "env": "static",
      "buildCommand": "cd guardian-eye-security-main && npm install && npm run build",
      "staticPublishPath": "guardian-eye-security-main/dist",
      "headers": [
        { "path": "/*", "name": "X-Frame-Options", "value": "DENY" }
      ],
      "routes": [
        { "type": "rewrite", "source": "/*", "destination": "/index.html" }
      ]
    }
  ]
}
